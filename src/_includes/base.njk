<!DOCTYPE html>
<html lang="en" data-theme="purple">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title }} — Hypermobility MD</title>
  {% if description %}<meta name="description" content="{{ description | truncate(160, true, '...') }}">{% endif %}

  {# SEO #}
  <link rel="canonical" href="{{ canonicalUrl or (site.url + page.url) }}">
  {% include "seo/open-graph.njk" %}
  {% include "seo/json-ld.njk" %}

  {# Podcast RSS feed #}
  <link rel="alternate" type="application/rss+xml" title="{{ site.podcastName }}" href="{{ site.podcastFeedUrl }}">

  {# Favicons #}
  <link rel="icon" href="/favicon.ico" sizes="48x48">
  <link rel="icon" type="image/svg+xml" href="/assets/favicon.svg">
  <link rel="icon" type="image/png" sizes="96x96" href="/assets/favicon-96x96.png">
  <link rel="icon" type="image/png" sizes="48x48" href="/assets/favicon-48x48.png">
  <link rel="icon" type="image/png" sizes="32x32" href="/assets/favicon-32x32.png">
  <link rel="icon" type="image/png" sizes="16x16" href="/assets/favicon-16x16.png">
  <link rel="apple-touch-icon" sizes="180x180" href="/assets/apple-touch-icon.png">
  <meta name="apple-mobile-web-app-title" content="Bendy Bodies">
  <link rel="manifest" href="/site.webmanifest">

  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link rel="preload" as="style" href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Playfair+Display:wght@600;700&display=swap">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Playfair+Display:wght@600;700&display=swap" rel="stylesheet" media="print" onload="this.media='all'">
  <link rel="stylesheet" href="/css/styles.css">
  {% if extraCss %}<link rel="stylesheet" href="/css/{{ extraCss }}">{% endif %}
  {% if needsRecaptcha %}
  <script src="https://www.google.com/recaptcha/api.js?render=6LejW5AsAAAAACTV9UzbWn_qHvRQXXH6oFtMxqhe" defer></script>
  {% endif %}
</head>
<body{% if bodyClass %} class="{{ bodyClass }}"{% endif %}>

{% include "nav.njk" %}

<main>
{{ content | safe }}
</main>

{% include "footer.njk" %}

{% block scripts %}{% endblock %}
</body>
</html>
